# SDMS API Specification

The PHP backend will expose a JWT-authenticated REST API over HTTPS. The complete endpoint contract is defined in [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md).

This document will be expanded with request schemas, response schemas, error formats, role permissions, and examples before frontend implementation begins.
